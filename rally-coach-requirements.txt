# rally-coach — environment captured from Documents\ChatGPT\_to_delete\Rally.venv
# Reconstructed from site-packages .dist-info metadata (pip.exe is a Windows
# binary and this session's shell is Linux, so 'pip freeze' could not be run).
# Source venv Python: 3.10.11
# Captured: 2026-08-13
#
# NOTE: this lists INSTALLED packages, which is what pip freeze reports too.
# Editable/local installs, if any, will not resolve from PyPI — check before
# assuming a clean 'pip install -r' on the MacBook.

absl-py==2.5.0
certifi==2026.7.22
cffi==2.1.1
contourpy==1.3.2
cycler==0.12.1
flatbuffers==25.12.19
fonttools==4.63.0
kiwisolver==1.5.0
matplotlib==3.10.9
mediapipe==1.0.0
numpy==2.2.6
opencv-contrib-python==5.0.0.93
opencv-python==5.0.0.93
packaging==26.3
pillow==12.3.0
pip==23.0.1
pycparser==3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
setuptools==65.5.0
six==1.17.0
sounddevice==0.5.5
