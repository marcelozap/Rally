# Pinned floor versions. `pip install -e ".[dev]"` is preferred; this is the
# fallback if the editable install gives you trouble on Windows.
opencv-python>=4.10
# PINNED EXACTLY, not a floor. 0.10.32 removed `mp.solutions` entirely, which is
# the API pose/mediapipe_backend.py is written against — an unbounded `>=`
# resolves to it on a fresh install and raises AttributeError on the first
# frame. Unpin only together with the port to mediapipe.tasks.PoseLandmarker.
mediapipe==0.10.14
numpy>=1.26
scipy>=1.13
pydantic>=2.7
typer>=0.12
rich>=13.7
pyyaml>=6.0
