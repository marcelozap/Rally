# rally-coach — copy to .env and fill in. .env is gitignored.
# Nothing here is required for local analysis; these are for future integrations.

# Where finished analysis JSON gets published for rally-app to pick up.
RALLY_COACH_EXPORT_DIR=artifacts/runs

# Optional: override the pose backend without editing config/pipeline.yaml
# RALLY_COACH_POSE_BACKEND=mediapipe
